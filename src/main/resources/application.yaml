spring:
  application:
    name: AdvancedRetailBillingApp
  datasource:
    password: ${DB_PASSWORD}
    username: ${DB_USER}
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          secret: ${JWT_SECRET}
          expiration-ms: 86400000

razorpay:
  key:
    id: ${RAZORPAY_KEY_ID}
    secret: ${RAZORPAY_KEY_SECRET}
app:
  low-stock-threshold: ${LOW_STOCK_THRESHOLD:5}
  order-number-prefix: ${ORDER_NUMBER_PREFIX:INV}